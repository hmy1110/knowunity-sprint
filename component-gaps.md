# Component gaps

Running list of things built inline during a screen build because Storybook had no component for them. See CLAUDE.md and `.claude/skills/build-screen/SKILL.md` for the process this follows.

## Summary (`/summary`, built 2026-09-16)

- **`sessionStats` (Summary's XP/Score/time row)** — design-system.md §1 documents this as a hand-built, non-componentized pattern (same "chips"-misnamed-layer territory as the separate XP badge used on the 9 Learning-*/Primer-micDenied screens, a different pattern from this one). Built into code once on 2026-09-16, pulled the same day after Mia's review ("doesn't look right"), left "flagged-not-built." Rebuilt here as `StatBox` (×3: XP/SCORE/BLAZING) in `src/app/summary/page.tsx` per Mia's direct instruction (2026-09-16) to build it from the live Figma frame, using all three real exported icon assets (Group 2136139938/2136139930/2136139937) recolored from their hardcoded hex fills onto the bound tokens they numerically match. Figma's own 108.667px card width is an unbound literal ((358px content − 32px of gaps) ÷ 3); reproduced as `flex: 1` instead, the same normalize-the-literal call `ButtonGroup` already makes for its own 319px frame. The big numeral's "Narrow Bold"/"Condensed Heavy" font-width-axis variant is also unbound in Figma (no token expresses a variable-font width axis); approximated via CSS `font-stretch: condensed` rather than invented as a fixed px value. **Not confirmed against a second instance** — Session's result screens (which use the separate, smaller XP-badge pattern per design-system.md, not this box row) won't validate this specific component; only a second look at this same Summary pattern would.

- **`TextBlock` has no variant matching Summary's real headline size.** The real "Good session, Mia." instance binds raw primitives directly (`font-size/2xl` = 32px, `font-line-height/xl` = 36px, weight 700) rather than any named text style — itself a violation of this file's own semantic-binding convention, confirmed via `get_variable_defs` on the real node (no named text style in its bound-variables list matches those two primitives together). The closest real named style is `Headline L` (`--type-scale-headline-l`, 33px/36px/700 — the same kind of ~1px Figma-vs-tokens.json drift already documented elsewhere in this file, e.g. `TableCell`'s label). None of `TextBlock`'s 4 documented variants bind that style (`XL`→`display/m` 76px, `L`→`headline/xl` 44px, `M`/`S`→body scales) — confirmed via Storybook's own `docs-show`, not assumed. `TextBlock`'s own component doc already flags this exact risk ("check whether their actual headline treatment... matches any of the 4 textBlock sizes, so you're not fighting the system on your two highest-visibility screens"). Built inline instead of forcing a mismatched `TextBlock` variant or fabricating a 5th one — a plain `<p>` bound to `--type-scale-headline-l-*`, matching `ScoreBreakdown`/`TermResultList`'s own precedent for inline type-scale binding. Worth a real decision from Mia: either add a `TextBlock` variant that binds `Headline L`, or bind the real node's headline directly to `Headline L` in Figma so it's a real named style there too.

- **Mascot ground-shadow ellipse** — a small decorative oval Figma places under Knowie's feet on the Hero (`Ellipse 75`, not a component, not part of `MascotSlot` itself), flush against the bottom of the 120px mascot box (confirmed from the grid math: `mt-[101.13px]` + `h-[18.867px]` = 120px exactly). Bound to `--semantic-color-background-stacking` (`white` at 10% opacity, matching Figma's own fill). Briefly swapped to `--semantic-color-background-surface` on a hunch that 10% white read as "barely visible" — reverted once pixel math showed that hunch was wrong: 10% white composited over `background/page` (`#090c18`) equals `#22242f` exactly, i.e. `background/surface`'s own literal value, so the two tokens render pixel-identical here. `background/stacking` is the semantically correct one to keep bound, since it's what Figma's own node actually binds.

- **Mascot renders visibly larger than the real Figma instance, not fixed here.** Side-by-side pixel comparison against a fresh Figma screenshot (2026-09-16) confirms the `<img>` box itself is exactly 120×120px at the right position — matching Figma's own generated code (`h-[120px] w-[var(--illustration-1500,120px)]`) exactly, confirmed via the DOM, not just eyeballed. The visible *glyph* still reads bigger and lower relative to the headline than Figma's real render. This isn't a Summary-page bug: `MascotSlot`'s own component doc already flags that `public/images/standby.svg` (200×217 viewBox) is an independently-sourced flattened export, "not independently confirmed" to match Figma's real asset's own internal proportions/padding — and that's exactly what this comparison surfaces. Fixing it means replacing `public/images/standby.svg` (or auditing its internal padding) at the `MascotSlot` level, which affects every screen using it, not something to patch per-screen inside Summary. Flagged for Mia rather than worked around with a one-off scale transform on this instance.

- **`StatusBar` and `ArrowLeftIcon` promoted to shared components (2026-09-16).** Both were built inline for Summary first; Primer-intro's live frame needed the identical iOS-status-bar mock and the identical `arrow-left` topNavigation icon, so per design-system.md §1's repeated-pattern rule they're now `src/components/StatusBar/StatusBar.tsx` (with its own story) and `src/components/shared/icons.tsx`, and both screens import from there instead of keeping their own copies.

## Primer-intro (`/primer`, built 2026-09-16)

- **SPEC.md said the three bullet lines ("Explain 4 topics from section 1" / "Stuck? Knowie guides you" / "Explain out loud helps you score up to 20% higher on exams") "are not used" — the live Figma frame shows all three, and they're built here.** SPEC.md's own text: "the real instance's three extra caption-style lines... are not used." That's stale — pulling `get_design_context` on the currently-selected Primer-intro node (2026-09-16) shows a real `Bullets` section with all three rows, each icon + text, still present. Built to match the live frame per the same "Figma wins, live frame is authoritative over SPEC prose" call already made (and confirmed by Mia) on Summary's appBar. Not rendered through `TextBlock`'s `caption` prop (SPEC's original assumption) since these are three independent icon+text rows, not a single block's caption.
- **Second button is `Tertiary`, not `Secondary` — SPEC.md is stale here too.** SPEC.md: "a second, `variant='Secondary'` button reading 'I can't talk right now'." The real instance has no fill, no border, no shadow behind that button — just plain `text/primary`-colored text — which matches `Button`'s own `Tertiary` variant exactly (`getFill` in `shared/buttonVariants.ts`: `Tertiary` = no background, `text/primary` label) and not `Secondary` (which is filled). Built as `Tertiary`/`L`.
- **`ButtonGroup`'s `Vertical` variant can't be used here — it's hardcoded to Primary+Secondary, this pair is Primary+Tertiary.** Since the real second button is `Tertiary` (previous point), and `ButtonGroup`'s own source fixes its Vertical pairing to `Button variant="Primary"` + `Button variant="Secondary"` with no way to override either slot's variant, using it here would force an incorrect filled look onto "I can't talk right now." Built as two direct `Button` instances instead, with the same `--size-space-200` gap `ButtonGroup`'s own `Vertical`/`L` config already uses, so the spacing still matches. Worth flagging to Mia: `ButtonGroup`'s own doc comment says Vertical "reads as the right shape for a stacked... pair" based only on Summary's Primary+Secondary case — Primer-intro shows that assumption doesn't hold for every Vertical pairing in the file.
- **Mascot renders visibly larger than Figma here too** — same `MascotSlot`/`public/images/standby.svg` asset-fidelity gap already flagged under Summary above, confirmed recurring on a second screen rather than being a Summary-only artifact. Still not fixed here, for the same reason: it's a shared-component/asset issue, not a per-screen one.

## StudyPlan-notStarted (`/`, built 2026-09-16)

- **First built as just the AppBar + study-plan card (SPEC.md's own scope), then rebuilt to match the full live Figma frame per Mia's direct instruction (2026-09-16: "SPEC.md only describe components, you need to build the same screen as figma!").** The frame (node `13622:13162`) is a full pre-existing Knowunity roadmap screen — an "Art & Design" header with a dropdown chevron, a day/grade chip row, a vertical topic-node roadmap, a section divider, and a bottom tab bar with an avatar — none of it named in SPEC.md's own "Study plan entry" section, and none of it had a Storybook component before this build. Superseded the earlier "flag and skip" entry below with the gaps actually found building it.
- **`TopicNode` — new component, not a Figma component.** Figma's own layer tree has no named component for the roadmap's circular icon-node rows; it's a hand-built frame repeated 4 times on this one screen alone (2 active/magenta, 2 disabled/gray), which already meets design-system.md §1's repeated-pattern bar on its own. Built as `src/components/TopicNode/TopicNode.tsx` with its own story rather than copy-pasted inline 4 times. Confirmed from the real node's own generated code that only the ring color changes between active/disabled — all 4 instances reuse the identical `ai-quiz` icon asset, fixed `accent/magenta/bold` fill, regardless of state; `active` only swaps the ring border color, never the icon's. Ring geometry (96px, 8px border, 66px inner disc offset 7px) is unbound literals in Figma, reproduced as literals.
- **The Speak button is `Primary`, not `Secondary` — SPEC.md is stale here too, same pattern as Primer-intro's second button.** SPEC.md: "`Button` `variant='Secondary'` `size='S'`... `cta='Speak'`." The real instance's fill (`interactive/primary`, white/lavender) and label color (`interactive/on-primary`, dark) match `Button`'s `Primary` variant exactly (see `shared/buttonVariants.ts`'s `getFill`) — `Secondary` fills with `background/surface` instead, which this instance doesn't use. Built as `Primary`/`S`.
- **The exam-score chip reuses the real `Chips` component** (`color="pro"` `active` `size="XXS"`, text `"🔥 +20% exam score"`, both icon slots hidden) — matches the `Pro Status Tag` real-usage story exactly, just with different text, not a new pattern.
- **The card's mascot has no exact `MascotSlot` size match.** The live instance is a resized/scaled mascotSlot (`~64–86px` effective box, not any of the 4 real size tokens: XL=64, 2XL=120, 3XL=200, 4XL=320). Built as `size="XL"` (64px, the closest real token) rather than fabricating a new size — an approximation, not a confirmed match.
- **`arrow_forward` icon** — real Material-icon asset pulled directly via the Desktop Bridge plugin's `exportAsync` (Code Connect-mapped, no raw asset URL in `get_design_context`'s response), recolored from Material's own default `#1D1B20` to the real bound token this button's label uses (`interactive/on-primary`).
- **`chevron-down` and 2 of the 4 navbar icons bind a stray "text/secondary" Figma variable (`#ffffffad`), not our real `--semantic-color-text-secondary` (`#f5f3ffad`)** — confirmed via `get_variable_defs` on the whole node, both variables present and distinct. Same duplicate-variable gap this file already documents elsewhere (TextField's caption color, ScoreBreakdown's dot colors). Bound to our real token here rather than reproducing the stray value.
- **The header chip icons (`calendar`, `target-04`) bind `white` at 24% opacity — a genuine unbound literal, no token matches.** Reproduced as that literal (`fill="white" fillOpacity="0.24"`) rather than forced onto a nearby-but-wrong token.
- **Bottom navbar icons and the avatar image are generic, off-system assets, not this file's design tokens.** The navbar frame itself reads as a separate Material-style nav-bar pattern. `myai-chat`'s own bound fill was a literal brand purple (`#8E51FF`, matching no real accent token) — per Mia's instruction (2026-09-16), rebound to `--semantic-color-text-secondary` instead, a deliberate departure from the literal Figma value rather than a correction of a misread one, matching the other inactive nav icons' own treatment. The avatar is a real exported photo asset saved to `public/images/avatar-placeholder.png`. None of the navbar items have a defined destination anywhere in SPEC.md, so none are wired to navigation — decorative only for now.
- **`get_design_context`'s own response for the navbar was incomplete — confirmed, not assumed, via a direct Desktop Bridge plugin query.** Its JSON showed 4 nav icons + avatar, all wrapper layers misleadingly named `myai-chat`, with the 4th icon exported as `ai-quiz1`. Querying the live node tree directly (`figma.getNodeByIdAsync` + children) found a real 5th button (`trophy-02`) the earlier response missed. Built it in (`NavTrophyIcon`, from the trophy's own raw `vectorPaths` since `exportAsync` fails on that node — Figma reports `absoluteRenderBounds: null` for it, no rendered pixels, though `fills` still returns a real, near-invisible dark-navy `#1E1A4D` color), then **removed again the same day on Mia's explicit instruction ("delete the trophy icon!")** — back to the original 4 nav icons + avatar. Kept: the `target-04` highlight fix below, since that was a real correction to an icon still in use, not tied to the trophy.
- **The 3rd icon (`target-04`) is the highlighted/active tab, bound to a real literal `#A684FF` fill** — a stray Figma variable literally named `text/link`, distinct from this codebase's own `text/link` token at `#9178e6`. `get_design_context`'s response didn't surface this highlight at all. `NavTargetIcon` now uses `#A684FF` directly (2026-09-16, per Mia's instruction to match Figma exactly).
- **`NavAiQuizIcon`'s path had a transcription bug that silently broke its rendering** — `...H14ZM19.5V6.7998C19...` was missing a `9 ` (should be `...H14ZM19 9.5V6.79981C19...`). SVG doesn't error on this; the path parser just drops everything after the malformed `M`, so the icon rendered as two stray bars and a question mark with no document outline — no lint or type error caught it, only a visual diff against Mia's reference screenshot did. Re-pulled the exact path from the plugin's raw vector data and replaced it outright rather than hand-patching the typo, to avoid trusting the same transcription twice.
- **Lesson for future screens**: don't treat one `get_design_context` response as exhaustive for a node with many repeated child instances (like a nav bar) — cross-check via a direct plugin query when something looks generic/placeholder-ish (e.g. every icon sharing one wrapper layer name). And when hand-copying a long SVG path string, verify it renders as expected rather than trusting that a successful lint/build means the path data itself is intact.
- **Mascot renders visibly larger than Figma here too** — same `MascotSlot`/`public/images/standby.svg` asset-fidelity gap already flagged under Summary above, now confirmed on a third screen.

## Learning-idle (`/session`, built 2026-09-16)

- **First screen of Session actually built — one route, many internal sub-states per SPEC.md.** No `subState` scaffold added at this point since `idle` was the only sub-state that existed yet; the mic tap, "Skip," "Type instead," and "I don't know" actions were all visually correct but not wired to a destination, per CLAUDE.md's "don't design for hypothetical future requirements." (The scaffold was added once `recording` gave it a second real case — see below.)
- **This screen's own leftIconButtonOnly row really is X close + `ProgressIndicator`, unlike Summary/Primer's — confirmed directly, not assumed from precedent.** SPEC.md and `AppBar`'s own doc both describe "X close, progressIndicator in the stretchable slot" as the shared Primer/Learning/Summary pattern, but Summary and Primer's *live frames* turned out to actually use `arrow-left` with an empty slot once checked. Learning-idle's own live frame (node `13622:16490`) genuinely does use the real `x-close` asset plus a working progress bar — checked independently rather than assumed to match or diverge from the other two screens' resolved pattern.
- **The appBar's stretchable slot holds *two* things, not one** — `ProgressIndicator` (`Primary`/`16`/`25`) *and* the "badge (XP/lightning counter)" pattern side by side, the same hand-built icon+numeral pattern design-system.md documents as appearing on 9 Learning-*/Primer-micDenied screens (distinct from Summary's own boxed `sessionStats` row). This is the first of those 9 screens actually built, so it's inline here rather than a shared component yet — promote once a second Learning-* screen needs it, per design-system.md §1's repeated-pattern rule. Real lightning asset, recolored onto `accent/blue/on-subtle` + `accent/blue/subtle` (confirmed via the asset's own literal fills `#7BA8F2`/`#0A1635`, both exact token matches) — a different, more muted color pairing than Summary's boxed badge, which used `accent/blue/bold`/`on-bold`.
- **"Skip" text link in the header row — not in SPEC.md's `idle` component list, built to match the live frame.** Sits beside "Topics 1 of 4," separate from the bottom "I don't know" button SPEC.md already documents as the cold-skip action. Shown, not wired — no confirmed answer yet on whether it's a duplicate/alias for the same skip action or something else.
- **Copy is "Topics 1 of 4," not "Term X of 4" as SPEC.md assumed** — matches the live frame's real text exactly.
- **`MascotSlot`'s own component doc says Learning-* screens use `pose="approving"` at `size="3XL"` — the live frame confirms `approving` but shows a ~70px box, not 3XL's 200px.** Built as `size="XL"` (64px, the closest real token) per the same approximate-to-the-closest-real-size call already made for StudyPlan's card mascot — not a confirmed match, and this specific 70px scale doesn't match any of `MascotSlot`'s 4 real size tokens (XL=64, 2XL=120, 3XL=200, 4XL=320). SPEC.md's own assumed `pose="standby"` for `idle` was wrong either way — confirmed `approving` from both the component doc and this live frame independently.
- **`SpeechBubble`'s own `Prompt` default message already matches this screen's real copy exactly** ("Let's start. Explain the term "Inspiration" out loud, in your own words.") — no `message` prop needed, confirmed by reading the component source rather than assumed.
- **Mascot renders visibly larger than Figma here too** — same `MascotSlot`/`public/images/standby.svg` asset-fidelity gap already flagged under Summary/Primer/StudyPlan above, now confirmed on a fourth screen.

## Learning-recording (`/session`, built 2026-09-16)

- **`subState` scaffold added — `'idle' | 'recording'`, added at this point rather than pre-built during `idle`.** Tapping the idle mic now really calls `getUserMedia({ audio: true })` (per SPEC.md's "the mic really requests OS permission... and really records") and transitions to `recording` on grant; denial isn't handled mid-session (Primer's own mic-permission gate already covers that path before a student reaches Session). The real `MediaStream` is kept in a ref for a later `readyToSend` build to capture into, but nothing is recorded into a blob yet — there's no consumer for one until that screen exists.
- **`recording`'s own mic tap originally led back to `idle` (stopping the stream) as an interim stand-in, since `readyToSend` didn't exist yet — superseded once `readyToSend` was built** (see that section below): it now really transitions `recording → readyToSend` per SPEC.md, with a real captured `Blob`.
- **`MicButton`'s `Recording` state was already fully built and documented** (`StatusIndicator` + 12-bar waveform + "Tap to stop", all real bound tokens) — used directly with no new gaps, `<MicButton state="Recording" onClick={handleStopRecording} />`.
- **The screen's own real node adds a full-bleed `background/scrim` dim behind the bottom content that `Learning-idle` doesn't have.** Its own literal fallback hex reads opaque `#0a0a0a`, but the real bound token is `rgba(10,10,10,0.5)` (confirmed via `get_variable_defs`-equivalent reasoning: the fallback is just Figma's codegen writing the token's RGB channels without its alpha) — bound to `--semantic-color-background-scrim`, not the opaque literal. Built as a `position: absolute; inset: 0` layer so the mic button (later in DOM order, also positioned) still paints above it, rather than reproducing Figma's own oversized 956px/440px overlay dimensions literally.
- **Everything else (StatusBar, appBar row, "Topics 1 of 4"/"Skip", mascot+speechBubble) is identical to `Learning-idle`** — confirmed from the live frame, not assumed; no new divergences from SPEC.md found on this sub-state.

## Learning-ready to send (`/session`, built 2026-09-16)

- **Real audio recording and playback actually built now — SPEC.md's "AudioScrubber plays back that real audio" is no longer aspirational.** `idle`'s mic tap now starts a real `MediaRecorder` on the granted stream (not just requesting permission, as it did when only `idle`/`recording` existed); `recording`'s mic tap stops it, builds a real `Blob`/object URL from the captured chunks, and hands it to a hidden `<audio>` element `AudioScrubber` actually controls (`play()`/`pause()`, `state` driven by the element's own `onPlay`/`onPause`/`onEnded`). This was deliberately not built during `idle`/`recording` alone (no consumer existed yet) — built now that `readyToSend` gives the captured audio somewhere real to go, per the project's own "don't design for hypothetical future requirements, build it when the next real consumer exists" pattern already used for the `subState` scaffold itself.
- **This screen has 3 action buttons, not 2 — SPEC.md's `ButtonGroup variant="Horizontal"` (icon + primary, 2 slots only) can't fit them.** The live frame shows Redo (delete icon, destructive red), **Send** (check icon — SPEC.md said `cta="Submit"`, the real label is "Send"), and a third button, **Resume** (play/pause icon), not mentioned anywhere in SPEC.md. Built as 3 separate icon-button-plus-label columns instead of `ButtonGroup`. **"Resume"'s own behavior is unconfirmed** — no SPEC.md guidance on what it should do (resume/append to the same take? something else?) — rendered to match the frame, deliberately left unwired rather than guessed at, same treatment as `idle`/`recording`'s own unresolved "Skip" link.
- **No `ButtonIcon` variant produces Redo's destructive red-on-dark-red fill.** `Primary`/`Secondary`/`Tertiary` map to `interactive/primary` (white), `background/surface` (gray), and no fill respectively (confirmed in `shared/buttonVariants.ts`) — none match the real instance's `feedback/error/on-bold` fill + `feedback/error/bold` icon. Built inline instead, matching `ButtonIcon`'s own L-size shape (56px circle, 24px icon, bottom bevel shadow) with the real bound colors substituted in. Send and Resume *do* match real `ButtonIcon` variants exactly (`Primary`/`L` and `Secondary`/`L` respectively, confirmed by comparing each icon's own bound fill color against `getFill`'s output) and use the real component as-is.
- **`StatusIndicator`, `AudioScrubber`, and `ButtonIcon` were all already fully built, documented components** — used directly with zero new gaps on any of them (`StatusIndicator status="Ready"`, `AudioScrubber state="Default"|"Playing"`).
- **A `Scrim` gradient layer in Figma's own `middleContent` is positioned `bottom: -3001px`** — entirely outside the 844px frame, reads as a leftover/copy-paste artifact from a taller source frame rather than a real visible effect (confirmed absent from the real screenshot too). Not reproduced.
- **Everything above the bottom content (StatusBar, appBar row, "Topics 1 of 4"/"Skip", mascot+speechBubble) is identical to `Learning-idle`/`Learning-recording`** — confirmed from the live frame, not assumed.

## Learning-processing (`/session`, built 2026-09-16)

- **`readyToSend`'s "Send" button is finally wired — `readyToSend → processing`, per SPEC.md.** The real ~2-3s timer SPEC.md documents ("a real ~2-3s delay, then auto-advances to `result`") is *not* started yet — `result` isn't built, and a timer with nothing real to fire into would just be dead code with no visible effect. Added once a `result` branch exists to receive it.
- **The layout here is genuinely different from `idle`/`recording`/`readyToSend`, not a variant of the same hero block — confirmed from the live frame, not assumed from SPEC.md's component list alone.** The original prompt drops out of its mascot+tail `SpeechBubble` entirely and becomes plain right-aligned text (it's already been "said"; now it reads as a caption of what was asked); the just-submitted `AudioScrubber` appears below it; and a *second*, new mascot+bubble row appears below that — Knowie's own real-time reaction, bare `pose="thinking"` + `SpeechBubble state="Loading"`. SPEC.md's Components list names these pieces but not this restructuring.
- **`pose="thinking"` really is a bare instance, not routed through `MascotSlot`** — confirmed both by SPEC.md's own explicit note (already flagged as a design-system.md gap) and the live frame's own structure (a standalone `<img>`, not the `mascotSlot`/`.mascotSlotBase` wrapper every other pose here uses). Built as a plain `next/image` at 70×70 from the same `public/images/thinking.svg` `MascotSlot` itself already draws from for that pose — same asset, just not through the component.
- **`SpeechBubble state="Loading"` and `AudioScrubber` were both already fully built, documented components** — used directly, `AudioScrubber` reusing the exact same `audioUrl`/`isPlaying`/`audioElRef` state `readyToSend` already set up (same one recording, replayable from either screen, per that component's own doc comment listing both as real instances).
- **Everything above the bottom content that isn't part of the restructured hero (StatusBar, appBar row, "Topics 1 of 4"/"Skip") is identical to every other sub-state so far** — confirmed from the live frame.

## Learning-result-Recalled (`/session`, built 2026-09-16)

- **`processing`'s ~2-3s auto-advance timer is finally wired** — `processing → resultRecalled` after a fixed 2.5s `setTimeout`, matching SPEC.md's "a real ~2-3s delay, then auto-advances to `result`" now that a `result` branch exists to receive it. Cleared on unmount/re-entry via the `useEffect`'s own cleanup.
- **This screen reuses `processing`'s restructured hero layout almost exactly** (plain right-aligned prompt text, `AudioScrubber` beneath it, `items-end` alignment) — confirmed from the live frame, not assumed to carry over. The one real difference: the second row goes back to the normal `MascotSlot` wrapper with `pose="approving"` (not `processing`'s bare `pose="thinking"` `<img>`), paired with a `SpeechBubble state="Success"`.
- **Real conflict between SPEC.md and the live Figma frame — resolved by asking Mia directly, not silently picking a side.** SPEC.md's "How the mocked recall behaves" section explicitly states no transcript of what the student said is ever shown, since there's no real STT to produce one. The live frame's `Success` `SpeechBubble` shows exactly that: a `message` reading "You said: 'It's the spark that makes you want to create something'" — a fabricated transcript. This isn't SPEC.md under-describing something (the usual case this session); it's a direct conflict with an explicitly-reasoned constraint, so it was flagged via a direct question rather than resolved by the usual "Figma wins" default. **Mia's call: build it exactly like Figma, transcript line included.** `SpeechBubble`'s own `Success` state already supplies the check-circle icon/title color internally (`title="Nice!"`, `subtitle="Unaided"`) — only `message` needed to be passed.
- **`AudioScrubber` has no width/className prop** (confirmed via its Storybook doc — `state` is its only prop) — its Figma instance here measures `w-267px`, narrower than `readyToSend`'s, but that's just the component's own intrinsic content width rendering the same on both screens, not something to override. No gap.
- **Everything above the hero (StatusBar, appBar row, "Topics 1 of 4"/"Skip") and the bottom `Button variant="Primary" size="L" cta="Continue"` match SPEC.md and the live frame exactly.** "Continue" isn't wired yet — its real destination (the next term's `idle`, or `/summary` once all terms are done) isn't built.

## Learning-result-Hinted1 (`/session`, built 2026-09-16)

- **Real `termIndex` scripting finally wired — SPEC.md's "the 4 terms are scripted by index... term 1 resolves Recalled, term 2 Hinted" is no longer aspirational.** Previously every screen hardcoded term 1's own copy. Added a `TERMS` array (`Inspiration`/`Recalled`, `Divergent thinking`/`Hinted` — only these two, since Revealed/Skipped aren't built) and a `termIndex` state. `processing`'s auto-advance timer now branches by `TERMS[termIndex].outcome` instead of always landing on `resultRecalled`; `resultRecalled`'s "Continue" is now wired (`handleContinue`) to advance `termIndex` and return to `idle` for the next term, clearing the previous term's captured audio. This was the minimum real wiring needed to make Hinted1 reachable at all — without it, there's no way to arrive at Hinted1 from a real user action, which would leave it as an unreachable dead-end render, unlike every other sub-state built so far.
- **Confirmed per-term prompt copy from Hinted1's own live frame, not assumed to reuse term 1's "Let's start..." phrasing.** Term 2's real prompt is `Explain the term "Divergent thinking" out loud, in your own words.` — no "Let's start." prefix, since that's specific to the session's opening term. `idle`'s `SpeechBubble state="Prompt"` now takes an explicit `message={term.prompt}` (previously relied on the component's own default, which happened to match term 1 exactly — no longer safe once a second term exists).
- **`MicButton`'s real Idle instance here has a different caption — "Try again," not "Tap to speak."** `MicButton` had no way to override `STATE_CAPTION`'s fixed per-state text; added an optional `label` prop (falls back to the state default when omitted, so `Learning-idle`'s existing usage is unaffected) plus a new `IdleTryAgain` story. This is exactly the second real Idle instance the component's own doc comment already predicted ("Idle on Learning-idle and Learning-result-Hinted1") — fixing the component itself rather than a screen-local workaround, per design-system.md §1's repeated-pattern rule.
- **`SpeechBubble state="Warning"` real copy confirmed from the live frame**: `title="Not quite yet."`, `subtitle="Hint 1 of 2"`, `message="No worries. Think about the very first step, before you start narrowing down to your favourite idea."` The warning icon/title color are already bound internally by the component's own `Warning` state — no extra icon asset needed, same as `Success`'s check-circle on Recalled.
- **Not wired**: the "Try again" mic tap, "Type instead," "I don't know" (all match SPEC.md's real component list for this screen, `MicButton state="Idle"` + `Button variant="Tertiary"`, but have no real destination yet). Looping "Try again" into the existing `processing`/timer logic would incorrectly land back on this same Hinted1 screen instead of the real `Learning-result-Hinted2-succeed` destination, which isn't built — left unwired rather than wired to the wrong place.
- **This screen reuses `processing`/`resultRecalled`'s shared restructured hero layout exactly** (plain right-aligned prompt text, `AudioScrubber`, `items-end` alignment) — confirmed from the live frame, not assumed to carry over a third time. The mascot+bubble row goes back to the normal `MascotSlot` wrapper (like `resultRecalled`, unlike `processing`'s bare `thinking` pose).

## Learning-result-Hinted2-succeed (`/session`, built 2026-09-16)

- **Hinted1's "Try again" is now wired to a real re-attempt loop** — SPEC.md: "Re-attempt loops back through recording → readyToSend → processing, then resolves to the Hinted-success result." `handleRetry` starts a genuinely new `getUserMedia`/`MediaRecorder` capture (shares `startRecording` with the first attempt's `handleMicTap`, parameterized by `isRetryAttempt`), moving the *current* `audioUrl` into a new `firstAttemptAudioUrl` before the second recording overwrites it. `processing`'s timer now checks a new `isRetry` flag alongside `term.outcome`: `Hinted` + `isRetry` → `resultHinted2Succeed` (not a loop back to `resultHinted1`).
- **Fixed a latent bug this required surfacing:** `handleStopRecording` previously revoked the *previous* `audioUrl` inside its own `setAudioUrl` updater on every recording — harmless when only one clip ever existed at a time, but it would have silently revoked the first attempt's own object URL the instant the second recording finished, breaking its `AudioScrubber` before this screen could ever play it back. Removed the inline revoke; discarding a clip is now only ever explicit (`handleRedo`, `handleContinue`), and a single unmount-only cleanup effect (via refs, not a per-change effect) handles the leak-prevention case instead.
- **This screen needs two independently-playable `AudioScrubber`s, not one** — added a second isPlaying/audioElRef pair (`isPlayingFirstAttempt`/`firstAttemptAudioElRef`) scoped to the first attempt's clip; the existing `audioUrl`/`isPlaying`/`audioElRef` continues to mean "the current/latest attempt," reused as-is for the second scrubber.
- **The Hint 1 block here is NOT a second `SpeechBubble` instance — confirmed from the live frame (node 13713:14750), not assumed to just reuse Hinted1's own Warning bubble.** It's the same icon+title+subtitle+message content with no bubble background, no tail — "collapsed to plain text," matching SPEC.md's own description of this exact treatment. Hand-built inline reusing `SpeechBubble`'s own internal Header styling (traced from its source: `IconSlot` size `300`, 20px bold title on `text/warning`, 12px Inter subtitle on `text/secondary`) rather than adding a "headerless"/"chromeless" prop to the real component for a single-screen case.
- **Real content confirmed from the live frame**: the closing `SpeechBubble state="Success"` uses `subtitle="Hint 2 of 2"` (not "Unaided," unlike Recalled) and a `message` that states the term's actual definition ("Divergent thinking is generating as many different ideas as possible before narrowing down to one.") rather than echoing back anything "said" — a genuinely different message pattern from Recalled's, not a copy-paste of it.
- **"Continue" is wired with the same `hasNextTerm` guard as Recalled's** — SPEC.md: "Terminal, single 'Continue,' no further retry." Term 3 ("Visual hierarchy," Revealed) isn't built, so this and Recalled's Continue both no-op past term 2 until `TERMS` gains a third entry.

## Learning-result-Hinted2-processing (`/session`, built 2026-09-16, renamed from Learning-result-Hinted1-processing)

- **New screen Mia added to Figma mid-session** (node 13727:15363), sitting between `Learning-result-Hinted2-recording` (below) and `Learning-result-Hinted2-succeed` in the flow — the real "processing" beat for a re-attempt, distinct from the generic `Learning-processing` frame used on a first attempt. `handleSend` branches on `isRetry`: a first attempt still goes to `processing`, but a re-attempt (after Hinted1's "Try again") goes to this screen instead.
- **Confirmed from the live frame, not assumed to reuse `resultHinted2Succeed`'s layout wholesale**: it turns out to be exactly `resultHinted2Succeed`'s own history stack (prompt, first attempt's `AudioScrubber`, Hint 1 collapsed to plain text, second attempt's `AudioScrubber`) with only the final row and bottom content swapped — bare `pose="thinking"` + `SpeechBubble state="Loading"` (matching `processing`'s own bare-thinking treatment, not the full `MascotSlot`) and `MicButton state="Processing"` instead of "Continue." Extracted the shared history stack into one `hintedHistory` JSX value reused by both screens, rather than a third copy-pasted block.
- **The auto-advance timer split into two independent branches** — `processing` (first attempt) still resolves by `term.outcome` alone; `hinted2Processing` always resolves to `resultHinted2Succeed` (scripted to always succeed on the re-attempt, per SPEC.md). `isRetry` no longer needs checking inside the timer itself, since a retry never reaches plain `processing` anymore — simplified back out of that effect's logic.
- **Renamed in code from `hinted1Processing`/"Hinted1-processing" to `hinted2Processing`/"Hinted2-processing"** after Mia renamed the underlying Figma frame and added a real `Learning-result-Hinted2-recording` screen before it in the flow — same node (13727:15363), same content, name-only change to keep the code's naming matching Figma's own numbering (this is genuinely the *second* hint attempt's own processing beat, not the first's).

## Learning-result-Hinted2-recording (`/session`, built 2026-09-16)

- **New screen Mia added to Figma** (node 13728:15704), the real missing link between `Learning-result-Hinted1` and `Learning-result-Hinted2-ready to send` — previously "Try again" jumped straight to the generic `recording` screen, which is now confirmed wrong: the real frame keeps Hinted1's own full `MascotSlot` + `SpeechBubble state="Warning"` row on screen (not collapsed — that only happens once `hinted2Processing`/`resultHinted2Succeed` are reached), adds the first attempt's own `AudioScrubber` above it, and swaps the bottom content for `MicButton state="Recording"` plus the same `background/scrim` dim `recording` itself uses (confirmed independently from this frame, not assumed to carry over).
- **`startRecording(isRetryAttempt)` now routes to `hinted2Recording` instead of the generic `recording` when `isRetryAttempt` is true** — the two `recording`-shaped screens (`recording` and `hinted2Recording`) share the same `MicButton state="Recording"`/scrim treatment via one combined render condition, but differ in their middle content (idle-style prompt bubble vs. the full Warning row + first-attempt scrubber).
- **Extracted `hintedWarningRow`** (mascot + full `SpeechBubble state="Warning"`) as a shared JSX value, reused by `resultHinted1`, `hinted2Recording`, and (below) `hinted2ReadyToSend` — confirmed identical content across all three live frames, not just visually similar.
- **`hinted2Recording`'s own `AudioScrubber` plays the *first* attempt, not the generic "current" one** — bound explicitly to `firstAttemptAudioUrl`/`isPlayingFirstAttempt`, not `audioUrl`/`isPlaying`. At this exact moment `audioUrl` still happens to equal the first attempt's own URL too (the second recording hasn't finished yet), so reusing the generic scrubber would have accidentally worked — deliberately used the explicit first-attempt state instead, since relying on that coincidence would silently break the moment `handleStopRecording` overwrites `audioUrl` with the second take.
- **Fixed a real, previously-undetected bug while touching this region**: the `<audio>` elements backing both scrubbers were mounted *inside* specific subState blocks (`readyToSend`'s own bottom content, and inside the `hintedHistory` value) rather than persistently — meaning `resultRecalled`'s, `resultHinted1`'s, and `processing`'s own `AudioScrubber` taps silently did nothing, since `audioElRef`/`firstAttemptAudioElRef` had already gone `null` the moment the mounting block's subState changed. Never caught before because no test in this session actually clicked those scrubbers, only checked their visual appearance. Fixed by mounting both `<audio>` elements once, persistently, right after `<StatusBar />`, guarded only by URL truthiness — confirmed working via a Playwright click on `resultHinted1`'s scrubber that now visibly toggles play state.

## Learning-result-Hinted2-ready to send (`/session`, built 2026-09-16)

- **A third new screen Mia added to Figma** (node 13728:15886), the real link between `Learning-result-Hinted2-recording` and `Learning-result-Hinted2-processing` — `handleStopRecording` previously routed every stop tap to the single generic `readyToSend`, which is now confirmed wrong for a retry: the real frame keeps the exact same `middleContent` as `hinted2Recording` (prompt, first attempt's `AudioScrubber`, `hintedWarningRow`), but its `bottomContent` is identical to the generic `Learning-ready to send`'s own — the second attempt's `AudioScrubber` (bound to the generic `audioUrl`/`isPlaying`, since it now correctly holds the just-finished second take), `StatusIndicator status="Ready"`, and the Redo/Send/Resume row. No scrim here, unlike `hinted2Recording` — confirmed absent from this live frame, not assumed to carry over.
- **`handleStopRecording` now branches on `isRetry`**: `readyToSend` for a first attempt, `hinted2ReadyToSend` for a retry. This is the third and last screen in the "keep the first attempt's history visible while X happens to the second" family (alongside `hinted2Recording` and `hinted2Processing`) — all three now share the same `firstAttemptAudioUrl`-bound top scrubber via one combined middleContent render condition.
- **This completes the real, distinct 3-screen retry sub-flow Mia asked for across three separate requests** (`Hinted2-recording`, then this, after "Try again" was first wired straight to the generic screens at each step and corrected once the real Figma frame turned out to differ) — `Hinted1 → Try again → hinted2Recording → hinted2ReadyToSend → hinted2Processing → resultHinted2Succeed`, with no step reusing a generic screen that Figma actually gives its own real frame.

## Learning-result-Revealed (`/session`, built 2026-09-16)

- **Real routing question resolved by reading the actual frame's own structure, not just SPEC.md's terse "term 3 resolves Revealed."** SPEC.md's `idle` section describes a generic cold-skip mechanic ("Tap 'I don't know' → result (Skipped, no AudioScrubber)"), which reads like it could apply to any term. But the live frame (node 13669:17091) has no cold-skip shape at all — its prompt has already dropped out of the `Prompt` `SpeechBubble`'s tail+bubble chrome into the same bare plain-text treatment `processing`/`resultRecalled` use once a real attempt has been submitted. Concluded term 3 really does go through the normal `recording` → `readyToSend` → `processing` chain and gets revealed directly from there (no hint offered first, unlike term 2) — added as a third `TERMS` entry (`'Visual hierarchy'`, outcome `'Revealed'`) and a third branch on `processing`'s own auto-advance timer, parallel to `Recalled`/`Hinted`.
- **No `AudioScrubber` on this screen at all** — confirmed absent from the live frame, not an oversight. Term 3's own attempt is revealed straight away, never replayed back. The shared middleContent block (`processing`/`resultRecalled`/`resultHinted1`/`hinted2Recording`/`hinted2ReadyToSend`) already branches its scrubber source by subState; added a `null` case for `resultRevealed` rather than reusing either existing scrubber source.
- **Real content confirmed from the live frame**: `SpeechBubble state="Error"`, `title="Here's the answer."`, `subtitle="Revealed"`, `message="Visual hierarchy arranges elements to guide attention and show what matters most."` — matches `SpeechBubble`'s own `Error` state defaults exactly, but passed explicitly for consistency with every other branch in this shared block. The term's own name, `"visual hierarchy"`, is genuinely lowercase in Figma's real copy (unlike `"Inspiration"`/`"Divergent thinking"`) — reproduced literally, not silently capitalized to match the other two.
- **"Continue" wired with the same `hasNextTerm` guard as Recalled's/Hinted2-succeed's** — SPEC.md's `result` section: "All result states: advance only on an explicit tap of 'Continue'... Last term's 'Continue' → `/summary` instead." Term 4 ("Visual research," Skipped) isn't built, so this still no-ops past term 3 until `TERMS` gains a fourth entry. `hasNextTerm` becoming true after term 2 (now that `TERMS` has 3 entries) also means `resultHinted2Succeed`'s own "Continue" is now a real, working transition for the first time — previously always guarded off since `TERMS` only had 2 entries.

## Learning-skipped (`/session`, built 2026-09-16)

- **This screen supersedes SPEC.md's whole term-4 description, not just one prop on it — flagging rather than silently picking a side, since both frames are still real and connected in Figma.** SPEC.md's Hinted1 section says term 4 "attempts once, lands on the hint-shown state, then skips from there" via `Learning-result-Hinted1`'s own "I don't know" button, landing on `Learning-result-I don't know` (node `13674:14198`, still present with its own connector from Hinted1's own button). But a separate, newer frame literally named `Learning-skipped` (node `13734:16233`) sits positionally right after `Learning-result-Revealed` in the canvas's own term-by-term sequence, with its own explicit connectors: `Learning-result-Revealed -> Learning-skipped -> Summary`. Built to match this newer, positionally-canonical frame — term 4 never routes through Hinted1 at all; it's structurally identical to `Learning-idle` (MicButton Idle, tailed `SpeechBubble state="Prompt"`, "Type instead"/"I don't know"), i.e. term 4's own cold-start `idle`, and its "I don't know" tap goes straight to `/summary` with no distinct result screen in between. Worth a direct check with Mia on whether `Learning-result-I don't know` should be deleted from Figma now that this exists.
- **UPDATE 2026-09-16, after a full Figma rename pass**: this frame is now named `Learning-topic 4-skipped` (same node `13734:16233`, content unchanged). Same rename scheme applied file-wide: `Learning-idle`→`Learning-topic 1-idle`, `Learning-result-Hinted1`→`Learning-topic 2-result-Hinted1`, `Learning-result-Revealed`→`Learning-topic 3-result-Revealed`, etc. — see SPEC.md's own naming note at the top of its Session section. Three further changes confirmed with Mia the same day: (1) term 3 is no longer required to attempt before Revealed — a new `Learning-topic 3-I don't know` frame (same idle-styled shape as this one) connects straight to `Learning-topic 3-result-Revealed`, so term 3 now needs the same code change this screen already made for term 4; (2) the second hint (previously cut) is back in scope — Figma now has a real `Learning-topic 2-result-Hinted2` fail terminal (hint 2 given, still wrong, Continue, no reveal) alongside the already-built success path, with its own retry sub-loop; (3) a "Skip" text link now sits top-right of every topic's appBar (next to "Topics X of 4") — per Mia, it's wired only on topic 4 (same destination as this screen's own "I don't know": straight to `/summary`), and stays inert on topics 1-3. None of these three are built yet as of this note.
- **No new `SubState` or JSX branch needed** — the existing `idle` render already used `term.prompt`/`termIndex` dynamically, so adding `TERMS[3]` (`'Visual research'`, outcome `'Skipped'`) was enough for the shared `idle` block to render this screen correctly on its own.
- **The mic button here is still mechanically live even though the canned/scripted path is the cold "I don't know" tap.** Since Figma has no distinct post-attempt result frame for term 4 (unlike Recalled/Hinted1/Revealed), an actual attempt now also routes straight to `/summary` from `processing` (added a `Skipped` branch to its own auto-advance timer) — the same terminal destination as the cold tap, rather than inventing a result screen Figma doesn't have.
- **Found and fixed a real bug in the shared appBar progress calculation, not just a Learning-skipped-specific value.** The old formula (`(termIndex+1)*25`) was only ever confirmed against term 1 (`Learning-idle`, 25%) and untested for terms 2-3, which have no separate `Learning-idle`-style frames of their own. Cross-checked the actual live fill fractions across four real screens instead of trusting the formula: `Learning-idle` (term 1) 25%, `Learning-result-Hinted1` (term 2) 50%, `Learning-result-Revealed` (term 3) 75%, `Learning-skipped` (term 4) *also* 75%, not 100% — matching design-system.md's own note that progress `100` has no real example anywhere in the file (reserved for Summary, once the whole session is actually done). Changed to `Math.min(termIndex + 1, 3) * 25` so the bar caps at 75% through the whole final term instead of jumping to an untested 100% mid-loop.

## StudyPlan-inProgress (`/`, built 2026-09-16)

- **Button label — SPEC.md is wrong, not just stale.** SPEC.md's "Study plan entry" section claims `cta="Redo"` on both `inProgress` and `finish`. The live `inProgress` frame (node `13622:18080`) still reads `cta="Speak"` — only `StudyPlan-finish`'s own designer note says its CTA demotes to "Redo." Built to match the live frame: `inProgress` keeps "Speak," routes to `/session` instead of `/primer`.
- **The in-card progress bar has no matching `ProgressIndicator` preset.** The live frame's bar is an 8px fill (`size/space/200`) inset 2px (`size/space/050`) on every side — shorter than both real `thickness` values the component supports (16 and 24; even 24's own 2px inset still yields a 20px fill, not 8px). Rather than force it into a mismatched preset, hand-built inline in `src/app/page.tsx` from the same real semantic tokens `ProgressIndicator` itself already uses (`--semantic-color-background-stacking` track, `--semantic-color-accent-brand-bold` fill, `--size-radius-full`). Progress is hardcoded to 50% (`2 of 4`, the real confirmed instance value) rather than computed, matching this project's "hardcode the real sample value now, wire it up once the real data exists" pattern (same as Summary's own sample result array). Only one screen uses this shape so far — not yet a repeated-pattern case per design-system.md §1, but worth promoting to a real `ProgressIndicator` variant if a second screen needs it.
- **"2 OF 4" is a separate text element next to the bar, not `ProgressIndicator`'s own `showText`/`unitText` mode** — same established pattern as the appBar's own "Topics X of 4" count sitting outside its progress bar. Styled with `--type-scale-caption-s-bold` (9px), the same token `Chips`'s own XXS size already resolved for this project's closest real precedent to Figma's raw `font-size-2xs` (8px) primitive — no semantic token exists at exactly 8px, so this reuses Chips' already-settled substitution rather than inventing a new one.
- **Cross-screen state derivation isn't wired.** SPEC.md describes the 3 study-plan-entry states as "derived from a local recall-session record, not fetched," but Session doesn't persist a record anywhere yet (same gap Summary had before Session existed to supply its real output). `/` (`src/app/page.tsx`) currently renders `notStarted` via a hardcoded `IS_IN_PROGRESS = false` constant; flip it (or wire it to a real record once Session writes one) to reach `StudyPlan-inProgress` instead. `StudyPlan-finish` isn't built yet.
- **Mascot pose — flagging a note-vs-frame conflict, not resolving it.** `StudyPlan-finish`'s own designer note says its mascot "sits in standby rather than approving" — implying `inProgress` (and `notStarted`) use an "approving" pose. But the live `inProgress` frame's own mascot layer is still literally named/rendered `standby`, identical to `notStarted`'s. Built to match the live frame (`standby`, unchanged from `notStarted`) per this project's "the frame is the visual source of truth" rule, but this specific note contradicts that frame rather than just being generically stale — worth a direct check with Mia rather than a silent resolution either way.

## Primer-micDenied (`/primer`, built 2026-09-16)

- **This screen has its own distinct appBar, not a shared one with Primer-intro — confirmed via the Desktop Bridge plugin, not assumed to carry over.** Primer-intro's own live frame uses `arrow-left` with an empty stretchable slot (already documented below under "Divergences"). Primer-micDenied's own live frame (node `13615:5335`) instead uses `x-close` plus a real `ProgressIndicator` + the XP-badge pattern — the same appBar shape Session's own Learning-* screens use, matching design-system.md's badge note ("9 screens... Primer-micDenied and every Learning-*"). Built to match: `CLOSE_ICON` + `ProgressIndicator`/badge, both copied inline from Session's own copies per that pattern's own "rebuilt independently on every screen" rule.
- **Progress is 25%, not the "0, untested" value SPEC.md assumed.** Measured directly off the real node: the fill rectangle is 67.25px inside a 269px track, exactly 25.0%. SPEC.md's own text flagged progress="0" as an assumption to verify before relying on it — verified and corrected rather than left as a guess. Conceptually odd for a pre-session screen to show 25% progress, so this may be a copy-pasted appBar instance from a Learning-topic-1 frame that was never reset — flagged for Mia, not silently changed to 0, since the live frame is what actually shows 25%.
- **The screen's headline body copy — "One tap in Settings, and you're back to speaking. Nothing else changes." — isn't in SPEC.md's component list for `micDenied` at all.** SPEC.md only names the `InlineAlert` title. Confirmed real from the live frame's own text node: raw 32px/36px/700 (`Greed VF-TRIAL`, `text/primary`), the identical unbound primitive combination Summary's own headline already resolved to `--type-scale-headline-l-*` (33px/36px/700, ~1px drift already documented there) rather than `TextBlock` — built the same way here for consistency, not re-litigated as a new gap.
- **`InlineAlert`'s real props confirmed via componentProperties, not re-guessed**: `variant="Warning"`, `showDescriptor={false}`, `title="Microphone access is off"` — exactly matches SPEC.md's own already-resolved note here (no new correction needed on this one prop set).
- **"Continue with text" is `Tertiary`, not `Secondary` — SPEC.md is stale here too, same pattern as Primer-intro's own second button.** Confirmed via the real instance's `componentProperties` directly (`variant: "Tertiary"`, size `L`), not just eyeballed — the live frame shows no button chrome at all behind that label. "Turn on my microphone" is confirmed `Primary`/`L`.
- **A `snackbar` instance sits in the live frame's layer tree with real copy** ("Microphone access is off. Nothing else about this session changes, turn it back on anytime from Settings.") plus a stray "Check it" button and an unrelated "1/2 words" chip — but the instance itself is `visible: false`, confirmed directly, not assumed. Doesn't conflict with `docs/sprint-context.md`/SPEC.md's "snackbar — not used anywhere" line, since it's hidden in Figma too. Not built.
- **A `Scrim` gradient layer in `middleContent` is also `visible: false`** — same leftover-artifact pattern already documented under `readyToSend`. Not reproduced.
- Mascot: `size="2XL"` `pose="standby"`, same 105×19px ground-shadow ellipse treatment as Primer-intro (identical dimensions, confirmed independently) — no new gap, reused as-is.
- Wired: `requestMic()` (renamed from `handleStartLearning`, since it's now shared by both "Start learning" and "Turn on my microphone") sets `screen: 'micDenied'` on `getUserMedia` rejection instead of silently doing nothing. "Continue with text" and "I can't talk right now" both route to `/session` — SPEC.md's own distinction between these landing in voice vs. text *mode* isn't wired, since Session itself has no mode parameter yet (out of scope for this screen alone).

## Divergences from the currently-selected Figma frame, not gaps

- **AppBar's left icon and progress value — resolved, SPEC.md may be stale here.** Originally built as an X-close icon plus `ProgressIndicator progress="100"`, per AppBar's Storybook doc (`Real Usage Primer Learning Summary` story) and SPEC.md's own text ("X close + ProgressIndicator progress=100"). Mia confirmed directly against the live Summary frame (2026-09-16) that topNavigation is actually just the `arrow-left` icon with no progress bar — corrected to match. Primer-intro's own live frame (node `13622:15919`) shows the identical `arrow-left`-plus-empty-slot shape, confirming this isn't a one-off on Summary. SPEC.md's Primer section and/or the `Real Usage Primer Learning Summary` Storybook story are out of date on this point and worth a follow-up correction.
